<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	<mapper namespace="com.goodee.everydoctor.report.ReportMapper">
		<insert id="setReport" parameterType="ReportVO">
			INSERT INTO REPORT (REPORTNUM, REPORTTYPENUM, REPORTID, REPORTEDID, 
			CONSULTNUM, ANSWERNUM, REPORTCONTENTS, REPORTCHECKED)
			VALUES (NULL, #{reportTypeNum}, #{reportId}, #{reportedId}, #{consultNum},
			#{answerNum}, #{reportContents}, #{reportChecked})
		</insert>
	</mapper>